apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "maybot.fullname" . }}
  labels:
    {{- include "maybot.controlCenter.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
