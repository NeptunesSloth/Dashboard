apiVersion: v1
kind: Service
metadata:
  name: {{ include "maybot.fullname" . }}
  labels:
    {{- include "maybot.controlCenter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "maybot.controlCenter.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
