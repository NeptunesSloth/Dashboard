apiVersion: v2
name: maybot
description: MayBot Control Center (and optional agent) for Kubernetes
type: application
# Chart version — bump on chart changes.
version: 0.1.0
# Default app version; overridable via image.tag in values.yaml.
appVersion: "latest"
home: https://github.com/neptunessloth/dashboard
sources:
  - https://github.com/neptunessloth/dashboard
maintainers:
  - name: MayBot
keywords:
  - maybot
  - monitoring
  - control-center
