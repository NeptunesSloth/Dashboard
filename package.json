{
  "name": "maybot-control-center-frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Opt-in Vite build pipeline (bundling/minification) for the MayBot Control Center frontend. Additive tooling only -- the FastAPI app still serves the source files in maybot_control_center/static/ by default.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "echo \"(placeholder) no JS linter configured yet\" && exit 0"
  },
  "devDependencies": {
    "vite": "^5.4.10"
  }
}
